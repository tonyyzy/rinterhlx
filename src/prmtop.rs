// prmtop parser and netcdf not implemented

// use std::fs::File;
// use std::io::BufReader;
// use std::io::prelude::*;

// pub fn parse(file: &str) -> std::io::Result<()> {
//     let file = File::open(file)?;
//     let mut buf_reader = BufReader::new(file);
//     let mut contents = String::new();
//     // buf_reader.
//     // buf_reader.read_to_string(&mut contents)?;
//     Ok(())
// }

// fn parse_pointer() {}

// fn parse_title() {}

// fn parse_atom_name() {}

// fn parse_atomic_number() {}

// fn parse_residue_label() {}

// fn parse_residue_pointer() {}

// CHARMM sections are ignored
